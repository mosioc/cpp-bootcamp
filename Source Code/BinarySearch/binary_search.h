#ifndef BINARY_SEARCH
#define BINARY_SEARCH

#include <vector>
#include <array>

bool binary_search(const std::vector<int>&, int /* search key */);
bool binary_search(const std::vector<int>&, int /* low */, int /* high */, int /* search key */);
bool binary_search(const int [], int /* low */, int /* high */, int /* search key */);

#endif // BINARY_SEARCH
